[project]
name = "cortex-contracts"
version = "0.1.0"
description = "Contract models for Cortex APIs."
requires-python = ">=3.12,<3.14"
dependencies = [
  "pydantic>=2.12,<3",
]

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"
