[project]
name = "cortex-parse"
version = "0.1.0"
description = "Parse orchestration for Cortex."
requires-python = ">=3.12,<3.14"
dependencies = [
  "PyYAML>=6.0,<7",
  "cortex-common",
  "cortex-contracts",
  "cortex-db",
  "cortex-domain",
  "cortex-observability",
  "cortex-storage",
  "httpx>=0.28,<0.29",
]

[project.optional-dependencies]
crawl4ai = [
  "crawl4ai>=0.7,<0.9",
]
llamaparse = [
  "llama-parse>=0.6,<0.7",
]
markitdown = [
  "markitdown>=0.1,<0.2",
]
markitdown-all = [
  "markitdown[all]>=0.1,<0.2",
]
docling = [
  "docling>=2.70,<3",
]
document-engines = [
  "docling>=2.70,<3",
  "markitdown[all]>=0.1,<0.2",
]
default-engines = [
  "crawl4ai>=0.7,<0.9",
  "llama-parse>=0.6,<0.7",
  "markitdown>=0.1,<0.2",
]
all-engines = [
  "crawl4ai>=0.7,<0.9",
  "docling>=2.70,<3",
  "llama-parse>=0.6,<0.7",
  "markitdown[all]>=0.1,<0.2",
]

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"
