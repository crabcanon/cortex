## YYYY-MM-DD - Optimize next sequence number calculation in jobs
**Learning:** Loading the entire list of job events using `await uow.job_events.list_for_job(job.job_id, limit=1000)` just to determine the next sequence number (`existing_events[-1].sequence_no + 1`) is highly inefficient and creates an N+1 query problem, especially for jobs with many events. Memory notes say: `The JobEventRepository provides an optimized get_max_sequence_no(job_id) method for calculating next sequence numbers using SQL aggregation. Use this instead of loading event lists into memory.`
**Action:** Add `get_max_sequence_no(job_id)` to `JobEventRepository` using SQL aggregation `func.max`, and update the usage in jobs in `packages/parse/src/cortex_parse/jobs.py`, `packages/synthesis/src/cortex_synthesis/jobs.py`, `packages/evaluation/src/cortex_evaluation/jobs.py`, and `packages/knowledge/src/cortex_knowledge/jobs.py`.
