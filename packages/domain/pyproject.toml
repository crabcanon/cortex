[project]
name = "cortex-domain"
version = "0.1.0"
description = "Domain layer for Cortex."
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"
